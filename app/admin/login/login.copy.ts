import { copy } from '@/locale';

export const titleLabel = copy.admin.login.title;
export const fieldLabels = copy.admin.login.fields;
export const submitLabel = copy.admin.login.submit;
export const submittingLabel = copy.admin.login.submitting;
export const errorLabel = copy.admin.login.error;
