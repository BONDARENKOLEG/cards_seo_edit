import { copy } from "@/locale";

export const titleLabel = copy.admin.products.title;
export const nameColumnLabel = copy.admin.products.columns.name;
export const statusColumnLabel = copy.admin.products.columns.status;
export const actionsColumnLabel = copy.admin.products.columns.actions;
export const editLabel = copy.admin.products.edit;
export const statusLabels = copy.admin.products.status;
export const filterLabels = copy.admin.products.filters;
