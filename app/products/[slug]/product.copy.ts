import { copy } from "@/locale";

export const backLabel = copy.productPage.back;
export const characteristicsLabel = copy.productPage.characteristics;
export const descriptionLabel = copy.productPage.description;
